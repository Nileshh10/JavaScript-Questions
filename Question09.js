//9. Use trim() to remove any trailing whitespace at the beginning and the end of a string.

 let str = '    Removes whitespace from both ends of a string and returns a new string, without modifying the original string    ';
  console.log(str);     //original
  console.log(str.trim());    //trim